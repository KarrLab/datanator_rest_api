type: array
items:
  type: object
  required:
    - inchikey
    - name
    - description
  properties:
    inchikey:
      type: string
    inchi:
      type: string
    description:
      type: string
    name:
      type: string
    reactions:
      type: array
      items:
        $ref: ./schemas/metabolites/reaction.yaml
